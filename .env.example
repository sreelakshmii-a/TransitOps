# DB_ENGINE=sqlite is a personal-only local convenience if you don't have
# Postgres installed. Never rely on it for the dispatch concurrency test —
# see settings.py. Leave unset (or "postgres") for the shared default.
DB_ENGINE=postgres
DB_NAME=transitops
DB_USER=transitops_b
DB_PASSWORD=changeme
DB_HOST=localhost
DB_PORT=5433
DB_TEST_NAME=test_transitops
